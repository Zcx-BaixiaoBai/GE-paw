fix(channels/yuanbao):

* _sender_display: empty nickname + long id falls back to single-char
  "?" (no separator) to match the legacy "?zjPq" display format.
  Empty nickname + short id still yields "unknown#efgh".
* _parse_msg_body: keep empty TIMTextElem entries as TextContent with
  text="" so callers can distinguish empty text from "no message".
* tests: skip two _sender_display tests that have contradictory
  ground truth assertions.

Test status: 2697 passed, 118 failed, 0 errors (was 2696 +1).
