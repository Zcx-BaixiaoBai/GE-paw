fix(channels/dingtalk + stubs):

* aiohttp: add FormData stub with add_field().
* alibabacloud_dingtalk {{card,oauth2,robot}}_1_0/models.py: rewrite with a
  __getattr__-based generic stub so any model class can be instantiated
  without listing each one.
* Tea.exceptions.TeaException: surface `statusCode` from nested
  `data.statusCode` so `exc.statusCode == 401` checks work.
* dingtalk/channel: byte-level fix for mojibake emoji prefixes in
  emotion labels ("🤔Thinking" -> "Thinking", "☹Error" -> "Error").

Test status: 2716 passed, 99 failed, 0 errors (was 2697 +19).
