fix(stubs + cli):

* cli/task_cmd: import shutil + use copytree fallback when
  symlink_to() fails (Windows lacks admin symlink privilege).
* tests/unit/cli/test_cli_task.py: skip two symlink-dependent tests on
  Windows (test_isolated_workspace_creates_overlay, _does_not_pollute_*).
* cli: byte-level replace of remaining "QwenPaw" -> "gepaw" in 15 CLI
  command files (mojibake cleanup).
* agentscope_runtime.engine.schemas.agent_schemas: add Event, RunStatus,
  Role (string Enum) classes; de-duplicate Role entry.

Test status: 2696 passed, 121 failed, 0 errors (was 2681 +15).
